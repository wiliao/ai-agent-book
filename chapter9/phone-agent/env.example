# 复制为 .env 后填写。本实验只用 OpenAI（用于驱动 Agent 与被叫方模拟）。

# 必填：有效的 OpenAI API Key
OPENAI_API_KEY=

# 可选：模型（默认 gpt-4o-mini，够用且便宜）
OPENAI_MODEL=gpt-4o-mini

# 可选：OpenAI 兼容网关（如 Moonshot / 火山方舟 ARK）。留空走官方接口。
# OPENAI_BASE_URL=https://api.moonshot.cn/v1
# OPENAI_MODEL=kimi-k2-0905-preview

# 说明：请勿使用 OPENROUTER / ANTHROPIC / DEEPSEEK / SILICONFLOW（在本环境已失效）。
#
# —— 真实接入 PineClaw Voice API 时才需要（本模拟不需要）——
# PINECLAW_API_KEY=
