# OpenAI API 配置（必填）：端到端 gpt-audio 与级联的 ASR(whisper-1)/TTS(tts-1) 都是
# 音频端点，只有 OpenAI 直连才有——OpenRouter 无音频端点，所以本实验必须提供 OpenAI 直连 Key。
# 从 https://platform.openai.com/api-keys 获取
OPENAI_API_KEY=your-openai-api-key-here

# 可选：级联中间的纯文本 LLM 思考默认模型 gpt-5.6-luna。若设置了 OPENROUTER_API_KEY，
# 该 LLM 思考会自动改走 OpenRouter（映射为 openai/gpt-5.6-luna），绕开 gpt-5.6* 直连
# 所需的组织实名认证；ASR/TTS/端到端音频仍走上面的 OpenAI 直连 Key。
# OPENROUTER_API_KEY=your-openrouter-api-key-here
# LLM_MODEL=gpt-5.6-luna

# 端到端 speech-to-speech 模型（可选，默认 gpt-audio）
# 需要该 Key 有权访问 gpt-audio；如有其他可用的 s2s 模型可在此覆盖。
# E2E_MODEL=gpt-audio

# 真实 Step-Audio R1（可选）：书中的端到端语音思考模型，无公开 endpoint，
# 需多卡 GPU 自行部署。部署后把服务地址写在这里，端到端一路即改走真实 Step-Audio R1；
# 留空则用上面的 gpt-audio 作为端到端代表。
# STEP_AUDIO_ENDPOINT=http://localhost:8000/v1/audio/chat
