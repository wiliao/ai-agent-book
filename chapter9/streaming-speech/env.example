# === 实验 9-3 环境变量示例：复制为 .env 后填入你的 Key ===

# 本实验用 OpenAI Whisper（whisper-1）做 ASR、OpenAI TTS（tts-1）合成测试音频。
OPENAI_API_KEY=your_openai_api_key_here

# 注意：书中原实验用 Qwen2-Audio，但其当前无可直接调用的 key/endpoint。
# 本实验只用音频端点（ASR whisper-1 / TTS tts-1），这类端点只有 OpenAI 直连才有——
# OpenRouter 只做聊天补全、没有音频端点，因此无法用 OPENROUTER_API_KEY 回退，必须用
# OpenAI 直连 Key。若只想验证分块/计时逻辑，用 python demo.py --offline 即可，无需任何 Key。
